services:
  - type: web
    name: cookscape-email-backend
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: node backend/dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: COMPANY_DOMAIN
        value: cookscape.com
